// The archive base name derives from the root project name, so the executable
// boot jar is built as build/libs/hinata-server-<version>.jar (matched by the
// Dockerfile COPY glob).
rootProject.name = "hinata-server"
